{% extends(':Admin:base.html.twig') %}
{% block content %}
    <p>
        <a href="{{ path('admin_dish_list') }}">
            <button class="btn btn-primary">Admin list/edit dishes</button>
        </a>
        <a href="{{ path('admin_dish_add') }}">
            <button class="btn">Add new dish</button>
        </a>
        <a href="{{ path('admin_dish_category_list') }}">
            <button class="btn btn-primary">Admin list/edit categories</button>
        </a>
        <a href="{{ path('admin_dish_category_add') }}">
            <button class="btn">Add new category</button>
        </a>
    </p>
{% endblock content %}
